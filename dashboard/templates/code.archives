// JS

// _data = new FormData(e.target.form)

// https://stackoverflow.com/a/17067016/3211506
// for (var pair of _data.entries()) {
//     console.log(pair[0]+ ', ' + pair[1]);
// }
// https://code.google.com/archive/p/form-serialize/


// https://gomakethings.com/how-to-get-all-parent-elements-with-vanilla-javascript/
elem = e.target;
for ( ; elem && elem !== document; elem = elem.parentNode ) { if (elem.tagName === "FORM") break }
